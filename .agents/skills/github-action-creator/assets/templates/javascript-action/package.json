{
  "name": "javascript-action-template",
  "version": "1.0.0",
  "private": true,
  "description": "Template for a Firebolt GitHub Action, written in TypeScript and bundled with Bun.",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build --target=node --outfile=dist/index.js src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
