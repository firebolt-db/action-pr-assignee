{
  "name": "@firebolt-db/action-pr-assignee",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Action that deterministically selects and assigns one PR assignee.",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build --target=node --outfile=dist/index.js src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/cache": "^4.0.2",
    "@actions/core": "^1.11.1",
    "@actions/github": "^9.1.1",
    "codeowners": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.18",
    "bun-types": "^1.3.13",
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
